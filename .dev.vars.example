# Wrangler local dev vars for `wrangler dev`.
# Copy to `.dev.vars` (do not commit real secrets).

SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR_SUPABASE_SERVICE_ROLE_KEY
SUPABASE_APP_DATA_TABLE=app_data
SUPABASE_APP_DATA_ID=default
# Recommended for production: uses normalized Supabase tables and avoids loading/saving
# the whole app_data JSON blob on high-traffic upload paths.
SUPABASE_USE_NORMALIZED=true

# Supabase Auth (JWT verification for social/phone login)
SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY

# Cline AI provider (primary — used for screening, HR interviews, PDF autofill)
# Get your key from https://api.cline.ai
CLINE_API_KEY=YOUR_CLINE_API_KEY
CLINE_API_URL=https://api.cline.ai/v1
CLINE_MODEL=Kimi K2.6 Code

# Fallback AI agents (server-side only; only used if CLINE_API_KEY is not set)
GROQ_API_KEY=YOUR_GROQ_API_KEY

# Runs the AI autopilot from the Worker cron trigger.
AI_AUTOPILOT_ENABLED=true

# Email delivery (optional, recommended for signup confirmations)
# Uses Resend (https://resend.com). In production set RESEND_API_KEY with:
#   npx wrangler secret put RESEND_API_KEY
RESEND_FROM="Helped <no-reply@yourdomain.com>"

# Dev-only: include confirmation code in API JSON responses (never enable in production).
DEV_EXPOSE_CONFIRMATION_CODE=false

# Make.com webhook for workflow automation (interview sessions, inquiry pipeline, etc.)
MAKE_WEBHOOK_URL=https://hook.eu1.make.com/your-webhook-url
