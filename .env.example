# Used only by the legacy Node/Express backend in backend/
DATABASE_URL=postgresql://user:pass@host:port/db
PORT=3000

# Cloudflare Worker deployment does not use DATABASE_URL.
# It uses the APP_DATA KV namespace configured in wrangler.toml.
